version: '3.8'
services:
  postgres:
    image: ${REGISTRY_PREFIX:-docker.m.daocloud.io/library}/postgres:15-alpine
    environment:
      POSTGRES_DB: ai_quant
      POSTGRES_USER: ai_quant_user
      POSTGRES_PASSWORD: AiQuant2024!
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports: ["5432:5432"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ai_quant_user -d ai_quant"]
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    image: ${REGISTRY_PREFIX:-docker.m.daocloud.io/library}/redis:7-alpine
    command: redis-server --appendonly yes --maxmemory 512mb --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    ports: ["6379:6379"]

  app:
    build:
      context: .
      args:
        REGISTRY_PREFIX: ${REGISTRY_PREFIX:-docker.m.daocloud.io/library}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    environment:
      POSTGRES_HOST: postgres
      REDIS_HOST: redis
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY:-}
      QWEN_API_KEY: ${QWEN_API_KEY:-}
      MOONSHOT_API_KEY: ${MOONSHOT_API_KEY:-}
      GLM_API_KEY: ${GLM_API_KEY:-}
      BAIDU_API_KEY: ${BAIDU_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      TUSHARE_TOKEN: ${TUSHARE_TOKEN:-}
    ports: ["8000:8000"]
    volumes:
      - app_data:/app/data

volumes:
  postgres_data:
  redis_data:
  app_data:
